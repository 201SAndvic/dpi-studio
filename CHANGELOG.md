# 更新记录

版本号规则：大改（加功能、改结构）第二位 +1 并开一个新的小改序列；小改（修问题、微调）
在小改序列里递增。永远不到 `1.0`。

---

## 1.0

**开源首发版本**

- 项目整体以 **GPL-3.0** 开源（因为内置了 GPL-3.0 授权的 LSPatch 引擎）
- 仓库更名并对外发布：<https://github.com/201SAndvic/dpi-studio>
- 补充 README、THIRD-PARTY.md 第三方组件清单、RELEASE_SIGNING.md 签名与发版说明
- 签名材料从 `app/build.gradle.kts` 移到不进版本控制的 `local.properties`；
  未配置密钥时自动回退为 Debug 签名，保证任何人 clone 后都能编译
- 「设置 → 关于 → 在 GitHub 查看源码」接上真实仓库地址
- 功能与本版本号无关，与 0.32 完全一致

## 0.32

- 「保存当前环境」生成的 zip 里加入 `日志.txt`

## 0.31

- 屏幕信息支持重命名，截图文件名跟随自定义屏幕名
- 截图文件名带上前台应用名：`DPI工坊-<屏幕名>-<时间>_<应用名>.png`
- 应用内提示条改为自绘，显示时长 1.5 秒
- 修复副屏截图与标识显示的相关问题

## 0.3

- 主页「屏幕信息」改为二级菜单：显示 / 截图 / 重命名
- 「显示」可在指定屏幕上弹出大号标识，用于确认屏幕身份
- 「截图」支持 root → 无障碍服务 → 提示授权 三级兜底

## 0.22

- 二级页面补充转场动画（设置 → 本机显示尺寸、设置 → 关于等）
- 修复「跟随系统」选项的高亮状态

## 0.21

- 设置 → 本机显示尺寸改为独立页面，不再从引导第一步开始
- 该页面左上角改为 `< 返回`，并去掉应用名标题
- 修正返回逻辑：主页侧滑才退出到桌面，其余页面逐级返回

## 0.2

- 引导页与主页之间加入界面切换动画
- 引导页文案重写，去掉口语化描述
- 调整图标配色与引导页圆角、图标尺寸

## 0.19

- 引导页第四步「一切就绪」
- 引导页左上角去掉应用名

## 0.18

- 修复「保存成品 APK」点击崩溃，改为内置导出目录选择

## 0.17

- 「选择目标 APK」改为内置文件浏览器，默认定位下载文件夹

## 0.16

**修复「注入后界面只剩中间一块」的根本原因**

- 模块用 `ZipInputStream` + `zipEntry.size` 读 `assets/config.xml`，
  而 Java 默认写出的 DEFLATED 项 size 为 `-1`，模块读配置失败后静默回退到
  代码内置的默认值（`forceRound=1.0` / `roundRatio=1.0`），界面被裁成 `1/√2`
- `ApkRewriter` 改为把被替换的这一项写成 STORED，并提前写入 size 与 CRC
- 注入时额外写一份 `/sdcard/AppConfig/<包名>.xml` 作为更高的优先级来源

## 0.15

- 设置 → 保存当前环境：多屏信息 + 截图打包成 zip 存到下载目录

## 0.14

- 本机显示尺寸滑块改为「松手才应用」，拖动不再卡手

## 0.13

- 应用内置文件浏览器，替代车机上不可用的系统文件选择器

## 0.12

- 首次打开加入三步引导：欢迎 → 界面显示尺寸 → 一切就绪
- 注入页加入「推荐注入配置」，与引导里设定的尺寸联动

## 0.11

- 主页、注入页全面对标 Sukisu Ultra 的排版：标题左对齐、去掉信息行之间的分隔线、
  统一为深色线条图标

## 0.1

- 由「应用配置注入器 v5.1」重构而来
- 界面从 Material 3 的 View 体系整体重写为 Compose + Miuix
- 更新应用图标与名称，版本号重置为 0.1
- `applicationId` 保持 `com.appconfig.injector`，可覆盖安装旧版
